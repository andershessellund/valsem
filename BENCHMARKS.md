# Benchmarks

<!-- GENERATED by `pnpm bench` from bench/results/*.json — do not edit by hand. -->

Every number below is produced by `pnpm bench`, which runs the suites in `bench/suites/` on Node and on Bun (JavaScriptCore), writes one JSON file per runtime, and renders this file. Each section describes exactly what its suite measures. Cells show **node / bun**; a single value means the suite ran on one runtime. The rationale behind the designs these numbers reflect lives in [DECISIONS.md](DECISIONS.md).

| | node 26.3.0 (V8) | bun 1.4.2 (JavaScriptCore) |
| --- | --- | --- |
| machine | Apple M2 Pro, darwin arm64 | Apple M2 Pro, darwin arm64 |
| date, commit | 2026-09-06, `378feb5` | 2026-09-06, `378feb5` |
| valsem | 0.0.1 | 0.0.1 |
| immer / mutative / immutable / fast-deep-equal | 11.1.18 / 1.3.0 / 5.1.9 / 3.1.3 | 11.1.18 / 1.3.0 / 5.1.9 / 3.1.3 |

Methodology, applied by every suite: results are retained where the honest regime requires it (discarded results die in the nursery and flatter copy-on-write libraries), each row asserts that contenders agree on the answer where there is one, fixtures are built in fast-properties mode (`Object.fromEntries`), and a novel state is produced per iteration unless the row says otherwise.

## produce — against immer and mutative

The update API, against the two libraries it is shaped after, at their shipped defaults plus immer with
auto-freeze off. Every iteration produces a **novel** state (the value written is the iteration counter),
except the recurrent row. Arenas:

- **big-array**: `{ arr: [10,000 records] }`, one record's field edited. *Synchronous* runs the produces back to
  back in one job — the regime that pays valsem's in-job WeakRef retention. *Held + macrotask* retains each result in
  a ring of 50 and runs one produce per macrotask, the regime a UI actually runs.
- **wide-record**: a 1,000-key record (built with `Object.fromEntries`, so the fixture is in fast-properties mode),
  one value edited. Every library's copy site is megamorphic by the time this arena runs, which is the honest state
  of a copy site in an application.
- **value-map / value-list**: a 10,000-entry `ValueMap` / 10,000-element `ValueList` edited through its draft
  (immer and mutative draft a native `Map` / array), and the same edit as a direct persistent operation.
- **small-churn**: a 3-key record, one field — the per-op floor.
- **recurrent**: the big-array arena cycling through 10 configurations, results held — the arena where a successor
  is a pure function of (canonical base, exact delta) and comes back pointer-identical.

|  | valsem | immer (autofreeze on) | immer (autofreeze off) | mutative | note |
| --- | --- | --- | --- | --- | --- |
| big-array 10k, one edit, synchronous loop | 19.8 µs / 3.03 ms | 629.8 µs / 2.27 ms | 3.1 µs / 7.7 µs | 3.0 µs / 8.9 µs |  |
| big-array 10k, one edit, held + one per macrotask | 9.9 µs / 3.23 ms | — | 6.6 µs / 6.1 µs | 6.6 µs / 6.8 µs |  |
| wide-record 1000 keys, one edit | 2.7 µs / 35.4 µs | 238.4 µs / 73.4 µs | 125.4 µs / 28.1 µs | 253.8 µs / 52.1 µs |  |
| value-map 10k, one set, through the draft | 6.5 µs / 3.3 µs | 467.8 µs / 152.8 µs | — | 402.5 µs / 22.9 µs |  |
| value-map 10k, one set, direct persistent op | 1.3 µs / 1.1 µs | — | — | — |  |
| value-list 10k, set + push, through the draft | 11.7 µs / 11.1 µs | — | 9.3 µs / 11.6 µs | 9.0 µs / 10.5 µs |  |
| value-list 10k, set + push, direct persistent ops | 3.3 µs / 3.7 µs | — | — | — |  |
| small-churn 3-key record, one field | 1.2 µs / 781 ns | 575 ns / 630 ns | 419 ns / 512 ns | 466 ns / 583 ns |  |
| recurrent: big-array cycling 10 configurations, held | 1.6 µs / 2.2 µs | 535.7 µs / 1.88 ms | 2.8 µs / 5.3 µs | 2.9 µs / 9.3 µs | valsem returns 10 pooled instances; equality afterwards is === |

## The boundary — admitting raw data

What it costs to make an API response canonical: an array of N records with 10 fields each (five strings, four
numbers, a boolean), as `JSON.parse` delivers it. Rows per size:

- **JSON.parse** of the same payload — a cost the application pays anyway; the yardstick.
- **structuredClone** and **immer's auto-freeze walk** (`produce(data, () => {})`) — the cheapest "make it
  immutable" walks a frontend runs today.
- **intern, all new content** — every record is novel: hashed, copied, pooled, frozen.
- **intern, unchanged refetch** — the same content arrives again; every record finds its pool entry and no copy is built.
- **intern, 10% changed** — a refetch with every tenth record different.
- **ValueList.from** of the same records — the list structure on top of admission.
- **RawArray.from + slice(0, 100)** — the way to *not* admit a large response: the view copies the array once and
  admits only the rows a slice asks for, so the cost is proportional to the window, not the response.

Values are per response; divide by N for the per-record cost. At 100 Mbit with 80% compression, the 1k response
transfers in ~2.7 ms and the 10k one in ~27 ms, off the main thread; admission runs on it.

|  | per response |
| --- | --- |
| 1000 × 10: JSON.parse | 387.2 µs / 372.4 µs |
| 1000 × 10: structuredClone | 740.6 µs / 453.5 µs |
| 1000 × 10: immer auto-freeze walk | 172.1 µs / 339.1 µs |
| 1000 × 10: intern, all new content | 2.06 ms / 907.6 µs |
| 1000 × 10: intern, unchanged refetch (all pool hits) | 941.5 µs / 615.1 µs |
| 1000 × 10: intern, refetch with 10% of records changed | 1.04 ms / 877.7 µs |
| 1000 × 10: ValueList.from, all new content | 1.90 ms / 1.19 ms |
| 1000 × 10: ValueList.from, canonical records (the list alone) | 84.8 µs / 96.1 µs |
| 1000 × 10: RawArray.from + slice(0, 100) — admit only the visible window | 238.1 µs / 166.2 µs |
| 10000 × 10: JSON.parse | 4.25 ms / 3.63 ms |
| 10000 × 10: structuredClone | 8.27 ms / 4.89 ms |
| 10000 × 10: immer auto-freeze walk | 1.85 ms / 3.63 ms |
| 10000 × 10: intern, all new content | 18.97 ms / 14.38 ms |
| 10000 × 10: intern, unchanged refetch (all pool hits) | 9.31 ms / 8.02 ms |
| 10000 × 10: intern, refetch with 10% of records changed | 11.10 ms / 14.97 ms |
| 10000 × 10: ValueList.from, all new content | 21.02 ms / 11.73 ms |
| 10000 × 10: ValueList.from, canonical records (the list alone) | 743.4 µs / 1.26 ms |
| 10000 × 10: RawArray.from + slice(0, 100) — admit only the visible window | 163.7 µs / 132.2 µs |

## deepEqual — against fast-deep-equal

Structural equality on raw data (the pure walk, fast-deep-equal's arena) and on canonical data (valsem's O(1)
short-circuit, which fast-deep-equal must still walk — over frozen objects, as its users would meet valsem values).
Every pair asserts that both libraries return the same verdict; the corpus avoids the two inputs on which their
semantics differ (`NaN` and undefined-valued keys). Rows:

- **raw =**: two independently built equal structures — the full walk.
- **raw ≠**: a difference planted in the middle.
- **canonical ≠**: two distinct canonical values — valsem answers from the canonicality probe without looking inside.
- **boundary**: raw wrappers around a shared canonical 100-item subtree — valsem terminates at the boundary.

|  | valsem | fast-deep-equal | fast-deep-equal ÷ valsem |
| --- | --- | --- | --- |
| record 10 keys: raw = | 336 ns / 128 ns | 248 ns / 104 ns | 1/1.4× / 1/1.2× |
| record 10 keys: raw ≠ | 211 ns / 75 ns | 164 ns / 55 ns | 1/1.3× / 1/1.4× |
| record 10 keys: canonical ≠ | 27 ns / 20 ns | 169 ns / 65 ns | 6.3× / 3.3× |
| record 100 keys: raw = | 4.9 µs / 1.0 µs | 4.6 µs / 967 ns | 1/1.1× / 1/1.0× |
| record 100 keys: raw ≠ | 2.3 µs / 479 ns | 2.7 µs / 598 ns | 1.2× / 1.2× |
| record 100 keys: canonical ≠ | 37 ns / 40 ns | 2.8 µs / 626 ns | 76.7× / 15.5× |
| record 1000 keys: raw = | 234.6 µs / 39.7 µs | 231.0 µs / 60.8 µs | 1/1.0× / 1.5× |
| record 1000 keys: raw ≠ | 90.6 µs / 8.1 µs | 134.5 µs / 36.4 µs | 1.5× / 4.5× |
| record 1000 keys: canonical ≠ | 68 ns / 13 ns | 132.8 µs / 20.5 µs | 1951.2× / 1521.6× |
| number array 10: raw = | 80 ns / 70 ns | 68 ns / 53 ns | 1/1.2× / 1/1.3× |
| number array 10: raw ≠ | 55 ns / 38 ns | 41 ns / 29 ns | 1/1.3× / 1/1.3× |
| number array 10: canonical ≠ | 29 ns / 13 ns | 76 ns / 125 ns | 2.6× / 9.9× |
| number array 100: raw = | 488 ns / 626 ns | 1.3 µs / 388 ns | 2.7× / 1/1.6× |
| number array 100: raw ≠ | 264 ns / 291 ns | 672 ns / 197 ns | 2.5× / 1/1.5× |
| number array 100: canonical ≠ | 29 ns / 17 ns | 581 ns / 953 ns | 20.1× / 55.2× |
| number array 1000: raw = | 4.6 µs / 4.4 µs | 13.1 µs / 3.7 µs | 2.9× / 1/1.2× |
| number array 1000: raw ≠ | 2.3 µs / 2.2 µs | 6.4 µs / 1.8 µs | 2.8× / 1/1.2× |
| number array 1000: canonical ≠ | 28 ns / 13 ns | 5.6 µs / 11.5 µs | 198.1× / 887.4× |
| array of 100 records: raw = | 16.6 µs / 7.3 µs | 11.3 µs / 5.4 µs | 1/1.5× / 1/1.3× |
| array of 100 records: raw ≠ | 8.4 µs / 3.7 µs | 5.6 µs / 2.4 µs | 1/1.5× / 1/1.5× |
| array of 100 records: canonical ≠ | 30 ns / 13 ns | 672 ns / 1.0 µs | 22.8× / 77.6× |
| boundary: raw wrappers, shared canonical payload = | 139 ns / 58 ns | 72 ns / 43 ns | 1/1.9× / 1/1.4× |
| boundary: raw wrappers, distinct canonical payloads ≠ | 142 ns / 51 ns | 788 ns / 1.1 µs | 5.6× / 20.6× |

## ValueMap, ValueSet, ValueList — against Immutable.js

The closest structural comparison: Immutable's `Map`/`Set` are HAMTs like valsem's; its `List` is a 32-way
radix vector, which valsem's `ValueList` was until it became a content-chunked tree. Primitive keys and values, so
intern-on-entry is a no-op and the trie work is what is timed; every row asserts both libraries agree. Per structure,
at N = 100 and N = 10,000:

- **build**, **get / has** (hit and miss), **set / add** (an existing key with a novel value, and a new key),
  **delete**, **iterate**.
- **update then hash**: one persistent update, then the hash of the successor — valsem pays hashing inside the update
  (consing), Immutable lazily on the first `hashCode()`.
- **equals =**: two independently built equal collections (shuffled insertion; for lists, `from` vs a push chain) —
  valsem's `deepEqual` is the `[interned]` short-circuit, Immutable's `is()` walks.
- **equals ≠**: equal but for one entry — cold, and again with Immutable's per-instance hash cache warmed, where
  `is()` short-circuits on differing cached hashes (an *equal* pair must still walk).

The last rows use record values, where the semantics differ by design: valsem canonicalises and freezes each record
(equal-content records are one instance and equal maps are the same object); Immutable stores references.

|  | valsem | Immutable | Immutable ÷ valsem |
| --- | --- | --- | --- |
| Map 100: build from entries | 63.1 µs / 66.8 µs | 6.5 µs / 6.5 µs | 1/9.7× / 1/10.2× |
| Map 100: get (hit) | 44 ns / 39 ns | 21 ns / 23 ns | 1/2.1× / 1/1.7× |
| Map 100: has (miss) | 42 ns / 56 ns | 25 ns / 62 ns | 1/1.7× / 1.1× |
| Map 100: set existing key → novel value | 1.9 µs / 1.2 µs | 145 ns / 151 ns | 1/12.8× / 1/7.9× |
| Map 100: set new key | 2.5 µs / 1.7 µs | 141 ns / 229 ns | 1/18.0× / 1/7.3× |
| Map 100: delete existing | 567 ns / 864 ns | 113 ns / 143 ns | 1/5.0× / 1/6.0× |
| Map 100: iterate entries | 1.2 µs / 2.1 µs | 1.3 µs / 2.1 µs | 1.1× / 1/1.0× |
| Map 100: set one key, then hash | 2.0 µs / 1.4 µs | 3.1 µs / 22.8 µs | 1.6× / 16.7× |
| Map 100: equals = (independent builds) | 7 ns / 11 ns | 3.4 µs / 8.9 µs | 487.6× / 815.8× |
| Map 100: equals ≠ (one entry differs), cold | 30 ns / 36 ns | 464 ns / 653 ns | 15.4× / 18.0× |
| Map 100: equals ≠, Immutable hashes warmed | 27 ns / 27 ns | 24 ns / 24 ns | 1/1.1× / 1/1.1× |
| Map 10000: build from entries | 11.91 ms / 13.65 ms | 943.9 µs / 1.07 ms | 1/12.6× / 1/12.8× |
| Map 10000: get (hit) | 74 ns / 68 ns | 65 ns / 71 ns | 1/1.1× / 1.0× |
| Map 10000: has (miss) | 66 ns / 100 ns | 63 ns / 119 ns | 1/1.0× / 1.2× |
| Map 10000: set existing key → novel value | 4.2 µs / 2.4 µs | 252 ns / 240 ns | 1/16.5× / 1/10.2× |
| Map 10000: set new key | 5.2 µs / 1.9 µs | 217 ns / 276 ns | 1/24.2× / 1/6.7× |
| Map 10000: delete existing | 1.5 µs / 1.3 µs | 212 ns / 864 ns | 1/7.2× / 1/1.6× |
| Map 10000: iterate entries | 146.7 µs / 213.6 µs | 179.5 µs / 265.0 µs | 1.2× / 1.2× |
| Map 10000: set one key, then hash | 7.1 µs / 2.8 µs | 352.6 µs / 970.9 µs | 50.0× / 349.3× |
| Map 10000: equals = (independent builds) | 11 ns / 41 ns | 626.6 µs / 830.6 µs | 54694.9× / 20187.1× |
| Map 10000: equals ≠ (one entry differs), cold | 41 ns / 49 ns | 101.3 µs / 153.0 µs | 2476.5× / 3141.6× |
| Map 10000: equals ≠, Immutable hashes warmed | 29 ns / 13 ns | 34 ns / 35 ns | 1.2× / 2.8× |
| Set 100: build from members | 41.8 µs / 42.3 µs | 6.9 µs / 6.4 µs | 1/6.0× / 1/6.6× |
| Set 100: has (hit) | 37 ns / 38 ns | 27 ns / 32 ns | 1/1.4× / 1/1.2× |
| Set 100: add new member | 2.2 µs / 775 ns | 141 ns / 486 ns | 1/15.7× / 1/1.6× |
| Set 100: delete existing | 462 ns / 757 ns | 121 ns / 150 ns | 1/3.8× / 1/5.0× |
| Set 100: iterate members | 974 ns / 1.4 µs | 1.3 µs / 1.4 µs | 1.3× / 1.1× |
| Set 100: add one member, then hash | 1.6 µs / 1.1 µs | 3.1 µs / 3.3 µs | 2.0× / 2.9× |
| Set 100: equals = (independent builds) | 7 ns / 8 ns | 4.3 µs / 3.7 µs | 611.8× / 447.4× |
| Set 100: equals ≠ (one member differs), cold | 28 ns / 16 ns | 3.4 µs / 2.9 µs | 121.0× / 181.8× |
| Set 100: equals ≠, Immutable hashes warmed | 27 ns / 13 ns | 34 ns / 47 ns | 1.3× / 3.7× |
| Set 10000: build from members | 10.78 ms / 10.33 ms | 938.3 µs / 1.04 ms | 1/11.5× / 1/9.9× |
| Set 10000: has (hit) | 72 ns / 68 ns | 70 ns / 63 ns | 1/1.0× / 1/1.1× |
| Set 10000: add new member | 4.2 µs / 1.7 µs | 239 ns / 297 ns | 1/17.7× / 1/5.8× |
| Set 10000: delete existing | 1.5 µs / 1.0 µs | 219 ns / 248 ns | 1/6.8× / 1/4.1× |
| Set 10000: iterate members | 108.8 µs / 175.5 µs | 153.6 µs / 236.9 µs | 1.4× / 1.3× |
| Set 10000: add one member, then hash | 2.8 µs / 2.4 µs | 349.4 µs / 345.3 µs | 123.3× / 145.9× |
| Set 10000: equals = (independent builds) | 8 ns / 54 ns | 640.4 µs / 856.3 µs | 81974.8× / 15734.4× |
| Set 10000: equals ≠ (one member differs), cold | 30 ns / 55 ns | 486.5 µs / 647.1 µs | 15965.8× / 11777.3× |
| Set 10000: equals ≠, Immutable hashes warmed | 28 ns / 13 ns | 57 ns / 40 ns | 2.0× / 3.1× |
| List 100: build from array | 4.1 µs / 5.8 µs | 3.1 µs / 2.6 µs | 1/1.3× / 1/2.2× |
| List 100: get (mid index) | 12 ns / 11 ns | 11 ns / 12 ns | 1/1.1× / 1.1× |
| List 100: set (mid) → novel value | 3.7 µs / 1.6 µs | 100 ns / 84 ns | 1/37.2× / 1/18.6× |
| List 100: push | 1.2 µs / 695 ns | 925 ns / 95 ns | 1/1.3× / 1/7.3× |
| List 100: pop | 257 ns / 410 ns | 54 ns / 67 ns | 1/4.8× / 1/6.1× |
| List 100: iterate elements | 700 ns / 987 ns | 1.7 µs / 1.3 µs | 2.5× / 1.3× |
| List 100: push one, then hash | 712 ns / 703 ns | 2.4 µs / 6.3 µs | 3.3× / 8.9× |
| List 100: equals = (from vs push chain) | 7 ns / 8 ns | 4.2 µs / 3.3 µs | 600.1× / 388.6× |
| List 100: equals ≠ (one element differs), cold | 32 ns / 17 ns | 2.2 µs / 1.8 µs | 68.1× / 110.6× |
| List 100: equals ≠, Immutable hashes warmed | 28 ns / 13 ns | 42 ns / 37 ns | 1.5× / 2.9× |
| List 10000: build from array | 398.6 µs / 1.05 ms | 368.0 µs / 328.3 µs | 1/1.1× / 1/3.2× |
| List 10000: get (mid index) | 17 ns / 11 ns | 13 ns / 13 ns | 1/1.3× / 1.2× |
| List 10000: set (mid) → novel value | 6.6 µs / 2.2 µs | 140 ns / 115 ns | 1/46.9× / 1/19.5× |
| List 10000: push | 1.8 µs / 522 ns | 134 ns / 101 ns | 1/13.7× / 1/5.2× |
| List 10000: pop | 442 ns / 84 ns | 71 ns / 81 ns | 1/6.2× / 1/1.0× |
| List 10000: iterate elements | 60.1 µs / 92.8 µs | 201.9 µs / 181.8 µs | 3.4× / 2.0× |
| List 10000: push one, then hash | 1.3 µs / 625 ns | 275.4 µs / 645.6 µs | 207.3× / 1033.0× |
| List 10000: equals = (from vs push chain) | 8 ns / 55 ns | 524.5 µs / 337.8 µs | 69476.6× / 6148.4× |
| List 10000: equals ≠ (one element differs), cold | 30 ns / 73 ns | 261.9 µs / 154.5 µs | 8596.5× / 2127.0× |
| List 10000: equals ≠, Immutable hashes warmed | 29 ns / 13 ns | 67 ns / 40 ns | 2.3× / 3.2× |
| Map 10k with record values: build from raw records | 30.31 ms / 21.27 ms | 1.52 ms / 1.61 ms | 1/20.0× / 1/13.2× |
| Map 10k with record values: build from canonical records | 23.69 ms / 20.98 ms | 1.02 ms / 1.17 ms | 1/23.2× / 1/18.0× |
| Map 10k with record values: set one raw record | 5.7 µs / 7.9 µs | 279 ns / 357 ns | 1/20.5× / 1/22.1× |

## HashMap and FastMap — against a native Map

`HashMap` matches keys by content: a key is hashed (`internHash` — O(1) on a canonical key, a walk on a raw one) and
compared structurally in a bucket table, and stored as given. `FastMap` is a native `Map` that admits canonical keys
only, where reference equality already is value equality: the canonical check costs a probe while checks are on, and
after `skipChecks()` the constructor returns a plain `Map`. Rows are hits on a 4,096-entry map unless marked; the
native `Map` column is what `FastMap` becomes with checks off.

|  | HashMap | FastMap (checks on) | native Map |
| --- | --- | --- | --- |
| get, canonical object key | 66 ns / 29 ns | 28 ns / 15 ns | 17 ns / 8 ns |
| get, primitive key | 55 ns / 27 ns | 25 ns / 10 ns | 26 ns / 6 ns |
| get, raw 2-key object (a walk; FastMap rejects, Map misses) | 321 ns / 257 ns | — | — |
| get, raw 50-record payload key | 13.3 µs / 7.2 µs | — | — |
| set, a novel raw 2-key object per call | 402 ns / 257 ns | — | — |

## memoize — hit and miss cost by argument kind

`memoize` keys on the argument tuple by content and interns results. The function is a selector over 100 records
(filter + map). Used as intended — canonical arguments, possibly with a small config literal built fresh beside them —
a hit is a cached-hash probe and `===` per argument. Handed raw payloads, it hashes and compares them structurally
on every call, which is the cost of skipping the boundary. The last rows isolate the hit cost against the width of
one raw record argument versus its canonical twin.

|  | per call |
| --- | --- |
| recompute, no memo | 257 ns / 496 ns |
| hit, both arguments canonical | 53 ns / 69 ns |
| hit, fresh filter literal + canonical list (the reselect case) | 253 ns / 287 ns |
| hit, fresh filter literal + ValueList | 286 ns / 159 ns |
| hit, both arguments raw (100 × 3-key records) | 31.2 µs / 17.6 µs |
| hit, maxSize 8, working set of 8 | 68 ns / 57 ns |
| miss + evict, maxSize 8, working set of 9 | 4.9 µs / 4.8 µs |
| hit, one raw 3-key record argument | 323 ns / 205 ns |
| hit, one canonical 3-key record argument | 33 ns / 44 ns |
| hit, one raw 20-key record argument | 1.2 µs / 766 ns |
| hit, one canonical 20-key record argument | 40 ns / 40 ns |
| hit, one raw 200-key record argument | 20.7 µs / 8.7 µs |
| hit, one canonical 200-key record argument | 41 ns / 38 ns |

## ValueList — operations and diff at 100k elements

`ValueList` is a content-chunked, hash-consed tree with the open last run kept in a tail array. A leaf boundary
falls after any element whose seeded hash says so (1 in 32; runs cap at 64), and branch runs follow the same rule on
node hashes, so the shape is a function of the content alone and an edit re-chunks only the runs around it. The first
block times each operation on a 100,000-record list (records canonical). The second block times
`ValueList.diff(a, b)` against a pointer scan over both `toArray()` snapshots (elements are canonical, so the scan
is already the cheap comparison): c point edits, an insert plus a remove, and a *refetch* — an independently built
list with three changed records, which shares nothing by lineage with the original.

|  | ValueList | pointer scan |
| --- | --- | --- |
| from, raw records (admission dominates) | 59.78 ms / 46.69 ms | — |
| from, canonical records | 12.54 ms / 20.97 ms | — |
| get, sequential | 16 ns / 10 ns | — |
| get, random | 72 ns / 87 ns | — |
| iterate for…of | 425.1 µs / 1.99 ms | — |
| toArray (memoized) | 515 ns / 115 ns | — |
| push | 2.1 µs / 746 ns | — |
| pop | 800 ns / 204 ns | — |
| set, middle | 7.7 µs / 2.7 µs | — |
| insert at 0 | 7.9 µs / 5.3 µs | — |
| insert, middle | 9.8 µs / 6.7 µs | — |
| remove at 0 | 3.1 µs / 5.5 µs | — |
| slice, middle half | 3.0 µs / 8.9 µs | — |
| concat, two halves | 6.7 µs / 5.1 µs | — |
| diff: 1 point edit (1 hunks) | 3.6 µs / 4.8 µs | 803.3 µs / 2.68 ms |
| diff: 10 point edits (10 hunks) | 12.4 µs / 21.7 µs | 802.2 µs / 2.60 ms |
| diff: 100 point edits (100 hunks) | 61.8 µs / 93.7 µs | 795.4 µs / 2.38 ms |
| diff: insert + remove (2 hunks) | 5.7 µs / 7.6 µs | 810.5 µs / 2.59 ms |
| diff: refetch, 3 changed, independently built (3 hunks) | 4.2 µs / 5.3 µs | 803.6 µs / 2.45 ms |

## ValueList inside produce — batched updates

One `produce` per row over a 100,000-record `ValueList`, through its draft. The draft never materialises: structural
ops go to a persistent working list as they happen, point edits and child drafts sit in an overlay by current index,
pushes wait in a tail, and finalize applies the overlay with `setMany` (one bottom-up pass that rebuilds each touched
leaf and ancestor once) and the tail as one splice. The last row is the direct persistent operations for reference.

|  | per produce |
| --- | --- |
| 1 set | 11.2 µs / 3.0 µs |
| 1 nested edit (get(i).v = x) | 12.6 µs / 3.2 µs |
| 100 sets, spread out | 630.7 µs / 402.9 µs |
| 100 nested edits, spread out | 665.6 µs / 396.5 µs |
| push 100 | 136.7 µs / 77.5 µs |
| 1 insert at n/2 | 14.4 µs / 23.8 µs |
| 10 inserts + 10 removes, spread out | 188.5 µs / 215.1 µs |
| 100 sets + 10 inserts + 10 removes | 825.1 µs / 826.8 µs |
| splice 1,000 out of the middle | 17.9 µs / 24.6 µs |
| pop 100 then push 100 | 193.4 µs / 116.4 µs |
| reference: direct set | 7.8 µs / 3.7 µs |
| reference: direct insert | 7.2 µs / 6.8 µs |

## Frozen arrays — what the engine charges for the frozen state

valsem freezes the plain arrays it canonicalises. The `Object.freeze` call is a map transition (~0.1 µs at any size);
the cost is the frozen **state**: engines have fast paths for frozen elements in some builtins and not in others.
The first row is the freeze call itself — a map transition on V8, an O(n) walk on JavaScriptCore, where it is the
dominant cost of canonicalising a large array. Each following row is one operation over a 10,000-element array,
unfrozen and then frozen, with one function per call site so the keyed loads stay monomorphic. Three element kinds: integers (Smi), doubles, and objects. This is the cost
`skipFreezing()` buys back in the user's own loops; `ValueList` never pays it (its leaves are unfrozen inside a frozen
wrapper).

|  | ints unfrozen | ints frozen | doubles unfrozen | doubles frozen | objects unfrozen | objects frozen |
| --- | --- | --- | --- | --- | --- | --- |
| the Object.freeze call itself (10k elements) | — | 202 ns / 1.85 ms | — | 243.2 µs / 1.75 ms | — | 214 ns / 1.74 ms |
| indexed loop read | 4.8 µs / 6.6 µs | 58.6 µs / 101.8 µs | 59.1 µs / 15.6 µs | 57.1 µs / 108.3 µs | 61.2 µs / 22.6 µs | 68.3 µs / 114.1 µs |
| for…of | 4.9 µs / 8.8 µs | 86.5 µs / 180.2 µs | 73.5 µs / 69.7 µs | 63.8 µs / 186.5 µs | 68.8 µs / 22.4 µs | 65.7 µs / 192.4 µs |
| forEach | 26.5 µs / 22.8 µs | 232.1 µs / 189.5 µs | 82.6 µs / 50.8 µs | 256.3 µs / 199.3 µs | 78.1 µs / 49.8 µs | 246.1 µs / 205.4 µs |
| map | 58.2 µs / 15.8 µs | 58.6 µs / 190.3 µs | 78.4 µs / 24.3 µs | 76.0 µs / 194.9 µs | 67.4 µs / 30.6 µs | 67.2 µs / 221.8 µs |
| filter | 80.2 µs / 173.0 µs | 262.0 µs / 348.3 µs | 200.7 µs / 173.3 µs | 401.4 µs / 358.4 µs | 197.1 µs / 181.4 µs | 428.6 µs / 356.6 µs |
| reduce | 55.7 µs / 9.2 µs | 55.1 µs / 189.8 µs | 74.1 µs / 24.3 µs | 69.3 µs / 197.8 µs | 91.3 µs / 26.9 µs | 90.9 µs / 210.3 µs |
| indexOf (miss) | 1.9 µs / 5.2 µs | 4.5 µs / 99.3 µs | 1.9 µs / 11.9 µs | 5.9 µs / 102.6 µs | 5.9 µs / 12.2 µs | 5.9 µs / 105.5 µs |
| slice() | 1.5 µs / 6.5 µs | 229.9 µs / 133.0 µs | 1.5 µs / 3.6 µs | 240.5 µs / 135.9 µs | 1.6 µs / 3.6 µs | 227.2 µs / 140.6 µs |
| spread [...a] | 1.4 µs / 8.7 µs | 1.4 µs / 134.5 µs | 1.4 µs / 8.5 µs | 1.4 µs / 143.4 µs | 1.5 µs / 9.7 µs | 1.3 µs / 142.3 µs |
| concat | 3.0 µs / 3.5 µs | 28.2 µs / 141.6 µs | 1.8 µs / 3.2 µs | 29.2 µs / 146.2 µs | 2.9 µs / 3.4 µs | 28.7 µs / 150.2 µs |
| JSON.stringify | 43.0 µs / 67.8 µs | 206.9 µs / 231.5 µs | 151.1 µs / 166.3 µs | 309.7 µs / 328.0 µs | 226.9 µs / 209.1 µs | 536.2 µs / 874.1 µs |
| at(-1) ×1000 | 921 ns / 2.2 µs | 19.7 µs / 11.1 µs | 3.5 µs / 6.1 µs | 20.1 µs / 13.0 µs | 3.5 µs / 4.4 µs | 21.0 µs / 13.0 µs |

## Copying a wide record — dictionary mode and megamorphic spread

Two engine facts that decide how fast a record copies, which set how `intern` builds canonical records and how
`produce` copies a draft. (1) An object grown by assigning keys one at a time into `{}` leaves fast-properties
mode at ~20 keys on V8; `Object.fromEntries` stays fast at any width — the first rows probe the mode where the
runtime exposes it (`--allow-natives-syntax`; otherwise they are omitted). (2) Object spread is an inline cache: a
memcpy while the site has seen ≤4 shapes, a per-property generic fallback after, and a library's single copy site
sees every shape in an application; `Object.assign` keys on the source map and stays fast at a megamorphic site.
Copy rows are for frozen fast-mode sources of 3, 20, 100 and 1,000 keys.

|  | spread, ≤4 shapes | spread, megamorphic | Object.assign, megamorphic |
| --- | --- | --- | --- |
| copy a 3-key record | 11 ns / 18 ns | 41 ns / 16 ns | 15 ns / 21 ns |
| copy a 20-key record | 38 ns / 23 ns | 241 ns / 23 ns | 40 ns / 28 ns |
| copy a 100-key record | 120 ns / 84 ns | 2.1 µs / 97 ns | 116 ns / 108 ns |
| copy a 1000-key record | 1.0 µs / 27.0 µs | 108.4 µs / 27.5 µs | 1.1 µs / 28.2 µs |

## Bundle size

esbuild, minified ESM, importing through the package name so `exports` and `sideEffects` are honoured; gzip at
level 9. Each row bundles one import (or a set) and nothing else. immer, mutative and Immutable.js rows are the
comparable entry points of those libraries. Node only.

|  | minified | gzipped |
| --- | --- | --- |
| valsem: produce | 23.9 KB / — | 8.6 KB / — |
| valsem: produce, current, original | 25.1 KB / — | 8.9 KB / — |
| valsem: deepEqual | 3.1 KB / — | 1.5 KB / — |
| valsem: intern | 11.0 KB / — | 4.5 KB / — |
| valsem: HashMap | 10.0 KB / — | 4.1 KB / — |
| valsem: ValueMap | 22.8 KB / — | 8.2 KB / — |
| valsem: ValueList | 28.0 KB / — | 10.2 KB / — |
| valsem: memoize | 13.3 KB / — | 5.3 KB / — |
| valsem: everything | 60.9 KB / — | 19.9 KB / — |
| immer: produce | 9.1 KB / — | 3.7 KB / — |
| immer: produce + enableMapSet | 12.0 KB / — | 4.6 KB / — |
| mutative: create | 18.7 KB / — | 6.2 KB / — |
| Immutable.js: Map | 64.0 KB / — | 18.1 KB / — |

## Reproducing

```bash
pnpm build
pnpm bench            # node, then bun, then this file
node bench/run.mjs list memoize   # one or more suites, merged into the node results
npx -y bun@latest bench/run.mjs  # the same on JavaScriptCore
node bench/report.mjs            # re-render BENCHMARKS.md
```

The one-off experiments behind the decisions — pool cleanup strategies, retention regimes, the in-job WeakRef effect — live in `scripts/experiments/` and are referenced from [DECISIONS.md](DECISIONS.md).
